package darkapi.script.utils;

import com.runemate.game.api.client.paint.PaintListener;
import com.runemate.game.api.hybrid.Environment;

import java.awt.*;
import java.util.ArrayList;

/**
 * Created by Aiden on 22/10/2015.
 */
/*public class PaintManager implements PaintListener {

    private static ArrayList<Paintable> paintables = new ArrayList<>();

    public PaintManager() {
        Environment
    }

    @Override
    public void onPaint(Graphics2D graphics2D) {
        ListIterator<Paintable> iterator = paintables.listIterator();
        while(iterator.hasNext()) {
            graphics2D.draw(iterator.next().getShape());
            iterator.remove();
        }
    }

    public static void submit(Paintable p) {
        if(!paintables.contains(p))
            paintables.add(p);
    }
}*/
